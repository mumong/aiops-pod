apiVersion: v1
kind: Service
metadata:
  name: workload
  labels:
    app.kubernetes.io/name: workload
spec:
  selector:
    app.kubernetes.io/name: workload
  ports:
    - name: http
      port: 8080
      targetPort: http

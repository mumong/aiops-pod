apiVersion: apps/v1
kind: Deployment
metadata: {name: workload}
spec:
  template:
    metadata:
      labels: {aiops.lab/case-id: c07}
    spec:
      containers:
        - name: app
          env:
            - {name: FAILURE_MODE, value: config_missing_exit}

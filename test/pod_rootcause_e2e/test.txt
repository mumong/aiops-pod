# RootCause E2E groups
# 取消注释哪个 group，就串行测试哪个 group。
# 全量测试：取消下面所有稳定 group 的注释。
#
# 全量后台执行（输出写入日志）：
# nohup .venv/bin/python test/pod_rootcause_e2e/run_rootcause_suite.py --scenarios-file test/pod_rootcause_e2e/test.txt -n 50 -c 2 --question "我的集群有什么问题" --url http://10.2.0.48:30800 > testreports/pod_rootcause_nightly.log 2>&1 &
#
# 单 group 前台执行（把 GROUP 换成 volumemount/pending/imagepull/crashloop/configerror/oomkilled/notready/terminating/sandbox/evicted）：
# printf "group:GROUP\n" > /tmp/rootcause_group.txt && .venv/bin/python test/pod_rootcause_e2e/run_rootcause_suite.py --scenarios-file /tmp/rootcause_group.txt -n 50 -c 2 --question "我的集群有什么问题" --url http://10.2.0.48:30800
#
# 查看后台进度：
# tail -f testreports/pod_rootcause_nightly.log
#
# 查看最新报告目录：
# ls -td testreports/pod_rootcause_suite_* | head -1

group:volumemount
group:pending
group:imagepull
group:crashloop
group:configerror
group:oomkilled
group:notready
# 可自动化 backlog，已接入 rootcause suite；如担心耗时或环境差异，可以先单独跑。
#group:terminating
#group:sandbox
#group:evicted

# Backlog（暂不建议夜间自动跑）
#group:unknown     # 需要停 kubelet/隔离节点
# evicted-noexecute-taint：可自动化但需要给节点打 NoExecute taint，暂不默认纳入。
# sandbox CNI/IPAM 类：依赖 CNI/IPAM 故障注入，暂不默认纳入。

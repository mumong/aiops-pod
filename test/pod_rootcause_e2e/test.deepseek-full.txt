# DeepSeek rootcause 全量自动化测试 selector
#
# 目的：
# - 用 DeepSeek 对当前所有“可以自动化复现”的 rootcause case 跑一轮完整评测。
# - 覆盖稳定夜间 group + 已自动化 backlog group。
# - 不包含 unknown、CNI/IPAM sandbox、节点压力 evicted、Node/kubelet 破坏性 terminating 等手动/破坏性场景。
#
# 预计覆盖：
# - 10 个 group
# - 26 个 case
# - 如果使用 -n 50，则共 1300 次诊断请求
#
# 建议执行前确认：
# - aiops-copilot 已切换到 DeepSeek 配置并完成 rollout
# - /health 可访问
# - aiops-e2e namespace 可正常创建/清理测试资源
#
# 后台执行命令见：
# - test/pod_rootcause_e2e/test.deepseek-full.commands.txt

# v1-core 稳定夜间 group
group:volumemount
group:pending
group:imagepull
group:crashloop
group:configerror
group:oomkilled
group:notready

# 已自动化 backlog group
group:terminating
group:sandbox
group:evicted

# 暂不纳入自动化：
# group:unknown
# sandbox CNI/IPAM/runtime 节点故障注入类
# evicted DiskPressure/MemoryPressure/PIDPressure/NoExecute taint 类
# terminating NodeNotReady/kubelet 无响应/CSI-NFS detach-unmount 类

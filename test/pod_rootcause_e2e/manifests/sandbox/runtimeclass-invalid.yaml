apiVersion: v1
kind: Pod
metadata:
  name: rc-sandbox-runtimeclass-invalid
  namespace: aiops-e2e
  labels:
    app: rc-sandbox-runtimeclass-invalid
    rootcause-e2e: "true"
    pod_abnormal_type: SandboxCreateFailed
  annotations:
    aiops.e2e/runbook: pod-sandbox-create-failed.md
spec:
  runtimeClassName: rc-definitely-missing-runtimeclass
  containers:
    - name: app
      image: busybox:1.36
      imagePullPolicy: Never
      command: ["sh", "-c", "sleep 3600"]

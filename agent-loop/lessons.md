# Lessons

- A budget estimator that only logs cannot prevent provider context errors.
- A compactor that requires another oversized LLM call needs a deterministic fallback.
- Cross-node state must be budgeted again; compressing one Agent's message history does not constrain downstream prompt assembly.


# 项目文档（入口）

这份仓库目前以“**FastAPI + HolmesGPT（Tool Calling）+ Runbooks（RAG）+ 第三方 MCP Server**”为核心。

建议阅读顺序：

1. `docs/ARCHITECTURE.md`（必读）：当前代码结构、调用链路、每个模块负责什么、你要改 runbooks/tools/prompts 应该去哪改。
2. `docs/RUNBOOK_INTEGRATION_GUIDE.md`：Runbook 的写法与维护方式（已按当前项目结构更新）。



# Authoritative evidence replay fixtures

These fixtures retain only generic canonical-record shapes and a deliberately
mutated model-owned entity ID. They contain no expected Case behavior and are
used only to replay evidence selection, claim validation, identity authority,
and deterministic final rendering offline.

Production policy must never branch on identifiers or values from this folder.

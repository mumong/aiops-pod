# K8s AIOps Copilot - 部署配置
# 注意：namespace 由 Makefile 的 deploy 目标创建（kubectl create namespace ... | apply），
# 这里不再声明 Namespace，避免 make delete 时误删 namespace。

apiVersion: apps/v1
kind: Deployment
metadata:
  name: aiops-copilot
  namespace: aiops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aiops-copilot
  template:
    metadata:
      labels:
        app: aiops-copilot
    spec:
      serviceAccountName: aiops-copilot
      initContainers:
        - name: fix-reports-permissions
          image: xnet.registry.io:8443/base/docker.io/library/busybox:1.36
          command: ["sh", "-c", "mkdir -p /tmp/aiops/reports/context_archives && chmod -R 777 /tmp/aiops/reports"]
          volumeMounts:
            - name: reports-volume
              mountPath: /tmp/aiops/reports
      containers:
        - name: aiops-copilot
          image: xnet.registry.io:8443/xnet-cloud/aiops-copilot:8.1.15
          imagePullPolicy: IfNotPresent # 每次都拉取最新镜像
          ports:
            - containerPort: 8000
          env:
            - name: LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: LLM_API_KEY
                  optional: true
            - name: LLM_MODEL
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: LLM_MODEL
                  optional: true
            - name: LLM_API_BASE
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: LLM_API_BASE
                  optional: true
            - name: MODEL_CONTEXT_WINDOW
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: MODEL_CONTEXT_WINDOW
                  optional: true
            - name: AIOPS_TOKENIZER_JSON_PATH
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: AIOPS_TOKENIZER_JSON_PATH
                  optional: true
            - name: AIOPS_TIKTOKEN_ENCODING
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: AIOPS_TIKTOKEN_ENCODING
                  optional: true
            - name: AIOPS_CONTEXT_USAGE_PROBE
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: AIOPS_CONTEXT_USAGE_PROBE
                  optional: true
            # AI 自动修复开关（true=允许执行修复操作，false=仅诊断）
            - name: AUTO_REMEDIATE
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: AUTO_REMEDIATE
                  optional: true  # 可选，默认 false（仅诊断）
            # 日志级别（DEBUG/INFO/WARNING/ERROR）
            - name: LOG_LEVEL
              valueFrom:
                secretKeyRef:
                  name: aiops-secret
                  key: LOG_LEVEL
                  optional: true  # 可选，默认 INFO
            - name: CONFIG_FILE
              value: "/app/config-override/config.yaml"
            - name: AIOPS_CONTEXT_ARCHIVE_ROOT
              value: "/tmp/aiops/reports/context_archives"
          volumeMounts:
            - name: config-volume
              mountPath: /app/config-override
              readOnly: true
            - name: runbooks-volume
              mountPath: /app/knowledge_base/runbooks
              readOnly: true
            - name: reports-volume
              mountPath: /tmp/aiops/reports
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      volumes:
        - name: config-volume
          configMap:
            name: aiops-config
        - name: runbooks-volume
          configMap:
            name: aiops-runbooks
        - name: reports-volume
          persistentVolumeClaim:
            claimName: aiops-reports-pvc

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aiops-reports-pvc
  namespace: aiops
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

---
apiVersion: v1
kind: Service
metadata:
  name: aiops-copilot
  namespace: aiops
spec:
  selector:
    app: aiops-copilot
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
  type: NodePort
